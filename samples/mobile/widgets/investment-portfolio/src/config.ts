export const config = {
  /** If the API is hosted on a different domain, update this URL.
   * This example is used for local development only, do not use this in production.
   */
  baseUrl: 'https://candescent.wiremockapi.cloud',
  
  /** The path to the API endpoint.
   * This example is used for local development only, do not use this in production.
   */
  apiPath: '/investment-portfolio',
};

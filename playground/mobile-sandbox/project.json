{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mobile-sandbox",
  "projectType": "application",
  "root": "playground/mobile-sandbox",
  "targets": {
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx run investment-portfolio:build --skip-nx-cache && CI=0 npm run start --workspace=mobile-sandbox -- --clear --port 8083"
      }
    },
    "android": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx run investment-portfolio:build --skip-nx-cache && CI=0 npm run android --workspace=mobile-sandbox"
      }
    },
    "ios": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx nx run investment-portfolio:build --skip-nx-cache && npm run ios --workspace=mobile-sandbox"
      }
    }
  }
}


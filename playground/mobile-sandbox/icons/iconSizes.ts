/**
 * Default Material icon sizes used across sandbox chrome (header, tabs, lists, FABs).
 */
export const SANDBOX_ICON_SIZE = {
  header: 24,
  tab: 24,
  rowLeading: 20,
  chevron: 24,
  /** Section label chevron beside title (e.g. Internal accounts). */
  sectionChevron: 24,
  /** Hosted widget dismiss (cancel) on home screen. */
  close: 26,
  fab: 24,
} as const;

/**
 * Tokens for placeholder / dashed widget-slot UI on the home screen (empty slots before pick).
 * Full registry widgets typically render their own card chrome.
 */
export const WIDGET_SLOT_MARGIN_VERTICAL = 4;
export const WIDGET_SLOT_BORDER_RADIUS = 8;
